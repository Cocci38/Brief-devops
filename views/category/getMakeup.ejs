<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/main.css">
    <link rel="stylesheet" type="text/css" href="/bootstrap/dist/css/bootstrap.min.css">
    <title>
        <%= title %>
    </title>
</head>

<body>
    <header>
        <%- include('../header') %>
    </header>
    <h1>
        <%= categoryMakeup.categoryName %>
    </h1>
    <div class="container text-center">
        <div class="row">
            <% productMakeup.forEach(function(product) { %>
                <div class="col">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= product.productName %>
                            </h5>
                            <h6 class="card-subtitle mb-2 text-body-secondary">
                                <%= product.productPrice %>€
                            </h6>
                            <p class="card-text">
                                <%= product.productDescription.substring(0, 100) + " ..." %>
                            </p>
                            <a href="/product/<%= product._id %>" class="card-link">Voir le produit</a>
                        </div>
                    </div>
                </div>
                <% }); %>
        </div>
    </div>
    <%- include('../footer'); -%>
</body>

</html>